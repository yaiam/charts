apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "power-ui.fullname" . }}-test-connection"
  labels:
    {{- include "power-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "power-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
