apiVersion: v1
kind: Service
metadata:
  name: {{ include "power-ui.fullname" . }}
  labels:
    {{- include "power-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "power-ui.selectorLabels" . | nindent 4 }}
